#include<bits/stdc++.h>
using namespace std;
typedef long long int ll;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);
    ll n, y, a=0, b=0, c;
    cin >> n >> y;
    c = y / 1000;

    if (c < n){
        cout << "-1 -1 -1";
        return 0;
    }
    
    while ((a + b + c) > n){
        
    }
    return 0;   
}