#include<bits/stdc++.h>
using namespace std;
typedef long long int ll;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);
    
    int n; cin >> n;
    vector<int> a(n), k(n);
    for (int i = 0; i < n; i++){
        cin >> a[i] >> k[i];
    }
    vector<int> (n);
    for (int i = 0; i < n; i++){
        
    }

    return 0;
}