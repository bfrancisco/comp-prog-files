#include<bits/stdc++.h>
using namespace std;
typedef long long int ll;

int n;
vector<ll> a, b;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);
    
    cin >> n;
    a.resize(n);
    b.resize(n);
    for (int i = 0; i < n; i++){
        cin >> a[i];
    }
    for (int i = 0; i < n; i++){
        cin >> b[i];
    }

    int q; cin >> q;
    while (q--){

    }
    
    return 0;
}