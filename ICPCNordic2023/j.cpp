#include<bits/stdc++.h>
using namespace std;
using ll = long long int;

int N, M;
vector<ll> items;
int main(){
    cin >> N >> M;
    items.resize(N+1);
    for (int i = 1; i <= N; i++){
        cin >> items[i];
    }

    

    return 0;
}